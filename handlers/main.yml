---

- name: restart sshd
  ansible.builtin.service: name=sshd state=restarted
  listen: "restart sshd"

- name: daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true
  listen: "daemon-reload"
